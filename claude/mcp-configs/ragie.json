{
  "mcpServers": {
    "ragie": {
      "command": "npx",
      "args": ["-y", "@ragieai/mcp-server", "--partition", "user-journey"],
      "env": {
        "RAGIE_API_KEY": "op://CLI/RAGIE/credential"
      }
    }
  }
}